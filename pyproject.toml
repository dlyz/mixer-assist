[project]
name = "mixer-assist"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastmcp>=2.0.0",
    "prompt_toolkit>=3.0.52",
    "python-osc>=1.10.2",
]

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
]
